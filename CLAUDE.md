# Landing Ángel Mendoza — entrada para Claude Code

La fuente de verdad del proyecto es `AGENTS.md`. Léelo y navega desde ahí.

@AGENTS.md

Protocolo compartido: `.agents/protocol/operating-protocol.md` (Regla del 95 %, Plan Mode,
ediciones quirúrgicas). Política por rol: `.agents/orchestrators/modelos.md`; bindings:
`.agents/config/models.json`. Comandos:
`/auto` `/auto-git` `/spec` `/exec` `/exec-git` `/exec-3` `/exec-3-git` `/research`. LF siempre ·
Español · Git en inglés (Conventional Commits).
